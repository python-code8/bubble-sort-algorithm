def bubblesort(list):
    # Swap the elements to arrange in order
    for i in range(len(list)-1,0,-1):
        for j in range(i): 
            if list[j]>list[j+1]:
                temp = list[j]
                list[j] = list[j+1]
                list[j+l] = temp

list = [19,2,31,45,6,11,121,27]
bubblesort(list)
print(list)
